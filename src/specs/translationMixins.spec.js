import { from } from 'core-js/fn/array'
import Vue from 'vue'


describe({'Profil.vue': () => {
    it('should render correct contents'), () => {
        const Contructeur = Vue.extend(age)
        const vm = new Constructor().age()
        expect(vm.$el.querySelector('.age').textContent).tobe(ager)
    }
},

})